[build]
  command = "npm install && npm run build"
  functions = "netlify/functions"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# Serve images directly as static files
[[redirects]]
  from = "/imgs/*"
  to = "/imgs/:splat"
  status = 200

# Serve contact page as static HTML for Netlify Forms to detect
[[redirects]]
  from = "/contact"
  to = "/contact.html"
  status = 200

# All other routes go to serverless function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/serverless"
  status = 200

